const cards = document.getElementsByClassName("card");

// let intervals = 0;
// let opacity = 1;
// let zIndex = 8;
// const setOpacityOfCards = () => {
//   Array.from(cards).forEach((x) => {
//     x.style.transform = `translate(${intervals.toString()}px)`;
//     x.style.backgroundColor = `rgba(25,25,25,${opacity})`;
//     x.style.zIndex = zIndex--;

//     intervals += 85;
//     opacity -= 0.125;
//   });
// };

// // Method invocations
// setOpacityOfCards();
